import Footer from '@/components/footer-2'; import { HeroHeader } from '@/components/header'; import { getLeaders } from '@/lib/public-data'
export const metadata={title:'About | UISS'}
export default async function AboutPage(){const leaders=await getLeaders(); return <div className="min-h-screen bg-canvas text-ink"><HeroHeader/><main className="mx-auto max-w-6xl px-6 py-20"><p className="text-sm uppercase tracking-widest text-muted">About UISS</p><h1 className="mt-4 text-5xl font-bold">Learn, build, and lead together.</h1><p className="mt-6 max-w-3xl text-xl leading-8 text-muted">The University of Dar es Salaam ICT Students’ Society connects students through practical learning, technical communities, projects, and leadership.</p><h2 className="mt-20 text-3xl font-bold">Current leadership</h2>{leaders.length?<div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{leaders.map(l=><div className="rounded border border-line p-5" key={l.id}><h3 className="font-bold">{l.firstName} {l.lastName}</h3><p className="mt-1 text-muted">{l.position.title}</p></div>)}</div>:<p className="mt-6 text-muted">Leadership profiles will appear here when published.</p>}</main><Footer/></div>}
